apiVersion: v1
kind: Service
metadata:
  name: backapp
  labels:
    homework: backapp
spec:
  type: ClusterIP
  selector:
    homework: backapp
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
