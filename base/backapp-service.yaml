apiVersion: v1
kind: Service
metadata:
  name: backapp
  labels:
    homework: backapp
    training: block4
spec:
  type: ClusterIP
  selector:
    homework: backapp
    training: block4
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
