apiVersion: v1
kind: Service
metadata:
  name: frontapp
  labels:
    homework: frontapp
    training: block4
spec:
  type: ClusterIP
  selector:
    homework: frontapp
    training: block4
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
