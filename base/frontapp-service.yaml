apiVersion: v1
kind: Service
metadata:
  name: frontapp
  labels:
    homework: frontapp
spec:
  type: ClusterIP
  selector:
    homework: frontapp
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
