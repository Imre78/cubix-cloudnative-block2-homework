apiVersion: v1
kind: Service
metadata:
  name: frontapp
  labels:
    app: frontapp
    homework: frontapp
    training: block3
spec:
  selector:
    app: frontapp
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP